package com.lingxi.goal.api;

/**
 * 优先级三档，目标与行动共用。
 *
 * <p>PRD 要求目标与行动都具备优先级，但未枚举取值；两者使用同一把尺子，
 * 才能真正支撑「今日工作台按需要先做排序」这类跨层判断，因此不各自定义一套同义枚举。
 */
public enum PriorityLevel {
  /** 低优先级。 */
  LOW,
  /** 常规优先级，未填写时的默认档。 */
  NORMAL,
  /** 高优先级，需要优先安排时间。 */
  HIGH
}
