<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingxi.platform.persistence.AsyncJobMapper">
    <select id="selectByBusinessKeys" resultType="com.lingxi.platform.persistence.AsyncJobEntity">
        SELECT *
        FROM plat_async_job
        WHERE (job_type, business_key) IN
        <foreach collection="jobs" item="job" open="(" separator="," close=")">
            (#{job.jobType}, #{job.businessKey})
        </foreach>
    </select>

    <insert id="insertBatch">
        INSERT INTO plat_async_job
        (id, job_type, business_key, status, payload_json, attempt_count, max_attempts,
         next_retry_at, progress, created_at, updated_at, version)
        VALUES
        <foreach collection="jobs" item="job" separator=",">
            (#{job.id}, #{job.jobType}, #{job.businessKey}, #{job.status}, #{job.payloadJson},
             #{job.attemptCount}, #{job.maxAttempts}, #{job.nextRetryAt}, #{job.progress},
             #{job.createdAt}, #{job.updatedAt}, #{job.version})
        </foreach>
        ON DUPLICATE KEY UPDATE id = id
    </insert>
</mapper>
