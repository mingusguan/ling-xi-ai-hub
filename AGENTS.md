## 文档与代码事实来源

- 当前用户已经确认的需求，以及 `状态: 当前有效` 或 `状态: 已确认` 的设计文档，用于描述目标业务行为。
- 当前代码、数据库结构、配置和测试用于描述实际实现状态。
- 文档模板只规定文档结构，不提供业务规则，也不能作为字段、枚举或实现行为的事实来源。
- 设计文档与代码不一致时，必须分别说明“设计规定”和“代码现状”，不得静默选择一方。
- 未经确认的内容统一放入“待确认事项”，不得写成当前有效规则。
- 待确认事项会明显影响接口、数据结构、事务边界、跨服务流程、兼容性或安全方案时，必须先向用户确认，再开始编码。

## Obsidian 文档位置

- 设计文档目录：`F:\笔记\个人知识库\灵犀\设计方案`
- 设计与交接模板目录：`F:\笔记\个人知识库\灵犀\设计方案\_模板`
- 接口文档目录：`F:\笔记\个人知识库\灵犀\接口文档`

## Obsidian 设计文档检索

当用户提出新版本、新功能、复杂业务改造、核心业务修改、接口设计或数据库变更时，编码前必须检索 Obsidian 设计文档。

简单 bug、格式修改、编译错误和纯局部改动不强制检索，除非改动影响已确认业务规则、接口语义、数据结构、事务边界或跨服务流程。

### 检索方法与预算

1. 根据模块名、业务词、类名、方法名、表名、接口名、服务名和代码路径提取关键词。
2. 先用 `rg` 搜索标题、YAML 属性、标签、关键词和命中行附近内容，不要一开始读取全部文档。
3. 检索业务设计文档时默认排除 `_模板` 目录；只有创建或更新文档时才读取模板。
4. 候选文档按照以下相关性排序：
   1. 接口名、类名、方法名、表名或代码路径精确命中。
   2. `功能`、`模块`、`涉及服务` 命中。
   3. `标签`、`关键词` 命中。
   4. 文档标题命中。
   5. 正文普通段落命中。
5. 同等相关性下，文档状态优先级为：
   1. `当前有效`
   2. `已确认`
   3. `已完成`
6. `已废弃` 文档默认不作为当前业务依据，只在追溯历史或判断兼容性时读取。
7. 默认全文读取最相关的 1 篇；只有约束分散时才扩展到 2-3 篇。
8. 候选超过 3 篇时，先只读取所有候选的 YAML 和命中行附近内容，再选择最相关的文档阅读全文。
9. 后续发现更精确的接口、类、表或代码路径命中时，可以替换相关性较弱的候选。
10. 创建文档时读取的模板不计入候选文档预算。
11. 新会话恢复时必须读取的版本总览、功能设计和实施交接不受“默认全文读取 1 篇”的限制。

### 检索结果输出

编码前简要说明：

- 最终读取或创建了哪些设计文档，以及选择原因。
- 当前有效的关键约束。
- 文档之间或文档与代码之间的差异。
- 尚未确认的问题。
- 准备采用的实现范围。

未命中时必须明确说明未找到相关设计文档，并进入“设计阶段”创建当前需求的设计文档。

## Obsidian 文档模板

### 设计方案模板

创建或补全设计文档时，必须根据设计粒度先读取对应模板：

- 版本设计：`F:\笔记\个人知识库\灵犀\设计方案\_模板\版本设计方案模板.md`
- 专题设计：`F:\笔记\个人知识库\灵犀\设计方案\_模板\专题设计方案模板.md`

模板选择规则：

1. 一个版本包含多个需求，需要统一记录版本范围、跨需求依赖、整体影响、实施顺序和版本验收时，使用版本设计方案模板。
2. 单个需求涉及跨服务、复杂状态机、独立数据模型、独立发布，或者正文过长不适合放入版本主文档时，使用专题设计方案模板。
3. 专题设计必须关联所属版本设计；版本主文档保留专题的核心结论、依赖、发布影响和验收入口，不得只保留一个无上下文链接。
4. 同一内容不得同时在版本设计和专题设计中维护两份完整规则；版本文档负责总览和跨需求约束，专题文档负责详细设计。

版本目录规则：

1. 新版本统一采用“一版本一目录”，目录为 `F:\笔记\个人知识库\灵犀\设计方案\版本需求方案\V<版本号>`；目录中的版本号统一保留一个大写 `V` 前缀，输入已经包含 `v` 或 `V` 时不得重复添加。
2. 当前版本的版本设计、关联专题设计和版本实施交接统一保存到该版本目录，不得直接散落在 `版本需求方案` 根目录。
3. 版本设计默认命名为 `「V<版本号>」设计方案.md`，专题设计默认命名为 `「V<版本号>-<需求编号>-<功能>」设计方案.md`，版本实施交接默认命名为 `「V<版本号>」实施交接.md`；文件名中的版本号同样只保留一个大写 `V` 前缀。
4. 接口文档和发版记录仍分别保存到既定的 `接口文档` 与 `发版记录` 目录，不随设计文档放入版本目录。
5. 已存在于 `版本需求方案` 根目录的历史版本文档默认保持原位置，不得仅为整理目录而移动；只有用户明确要求迁移并完成链接影响检查后才可移动。

执行规则：

1. 已有同一功能的设计文档时优先更新原文档，避免重复创建。
2. 没有对应文档时，以模板为基础创建新文档。
3. 新版本的版本设计与专题设计必须遵守上述版本目录和命名规则，不得保存到 `版本需求方案` 根目录或 `_模板`。
4. 保留模板 YAML 和一级章节；不适用的一级章节填写“无”，不适用的子章节可以精简或删除。
5. 根据实际模块、服务、接口、表和代码路径补全 YAML，保证后续可以通过 `rg` 检索。
6. 未确认内容必须放入“待确认事项”，不得写入“已确认业务规则”。
7. 模板中的示例、占位符和演示流程必须替换或删除，不得作为真实业务内容保留。

### 实施交接模板

创建或更新实施交接文档时，必须先读取：

- `F:\笔记\个人知识库\灵犀\设计方案\_模板\版本实施交接模板.md`

交接粒度规则：

1. 默认每个版本只维护一份当前有效的版本实施交接，作为新会话恢复和版本进度汇总的唯一入口。
2. 专题实施进度写入版本实施交接，并通过 `关联专题设计`、需求编号、代码路径和进度表关联对应专题设计文档。
3. 不为普通专题单独创建实施交接，避免版本交接与专题交接重复记录 Git 状态、测试结果、风险和下一步计划。
4. 只有专题跨版本持续推进、独立发布、由不同团队独立交付，或者其实施周期无法由版本交接可靠覆盖时，才考虑增加专题实施交接模板；新增前必须先向用户确认交接边界和维护责任。

执行规则：

1. 交接内容必须以当前设计文档、实际代码、Git 修改和测试结果为依据。
2. 必须区分已完成、部分完成、未完成和未验证事项。
3. 必须记录当前目标、代码路径、Git 状态、验证结果、文档与代码差异、已知问题、待确认事项和推荐下一步。
4. 默认命名为 `「V<版本号>」实施交接.md`，保存到当前版本目录，不得保存到 `版本需求方案` 根目录或 `_模板`；输入已经包含 `v` 或 `V` 时不得重复添加。
5. 同一版本存在进行中的交接文档时，优先更新原文档。
6. 只有阶段已经封存并且需要保留历史快照时，才创建带日期或阶段的新交接文档。
7. 未经确认的推测不得写入“已确认事项”。
8. 存在未完成或未验证事项时，不得将交接状态标记为 `已完成`。

## 设计驱动开发流程

对于新版本、新功能、复杂业务改造、核心业务修改、接口设计和数据库变更，采用设计文档驱动实现。

### 设计阶段

#### 设计澄清与决策确认

1. 新版本、新功能、复杂业务改造、核心业务修改、接口设计或数据库变更，在设计尚未确认或存在重大未决事项时，必须使用 `$design-grill`。
2. 使用 `$design-grill` 前，先按照本文规则检索设计文档并检查代码、数据库、配置和测试；能够从现有事实来源确认的问题不得询问用户。
3. `$design-grill` 每次只确认一个重要决策，并提供推荐方案及关键取舍。
4. 确认结果必须同步到对应的版本设计或专题设计文档；聊天结论不得作为唯一事实来源。
5. 涉及接口、数据结构、事务边界、跨服务流程、兼容性或安全的重大事项未确认前，不得开始编码。
6. 已有当前有效或已确认设计，且不存在新的重大决策时，直接按照设计实施，不得重复启动完整设计澄清。
7. 简单 bug、格式修改、纯编译错误、单纯测试和代码审查默认不使用 `$design-grill`，除非用户明确要求。

开始编码前：

1. 按照 Obsidian 检索规则查找相关设计文档。
2. 检查当前代码、接口、数据库结构、配置、测试和相关实现。
3. 没有当前需求的设计文档时，使用设计方案模板创建文档。
4. 已有文档时，根据已经确认的需求更新原文档，不为同一功能重复创建文档。
5. 使用模板记录背景、范围、业务规则、流程、接口、数据库、Redis、消息、定时任务、代码路径、异常边界、兼容性、验收、测试、设计决策和待确认事项。
6. 设计中存在会明显影响实现方式的待确认事项时，先向用户确认，再开始编码。

### 实现阶段

1. 将当前有效设计文档作为业务目标和约束来源。
2. 同时检查当前代码和 Git 修改，不能仅根据文档推断实现进度。
3. 开始实现前区分：已完成、部分完成、尚未实现、实现与设计不一致。
4. 不得重复实现已经完成的功能。
5. 实现过程中发现设计缺失、冲突或无法落地时，先说明问题。
6. 已确认的设计调整必须同步回 Obsidian。
7. 临时实现细节不得直接改写为当前有效业务规则。
8. 代码与设计不一致时，分别说明文档规定和代码现状。

### 完成与验收

功能实现完成后：

1. 运行相关编译、测试和静态检查。
2. 按照设计文档的验收条件逐项核对。
3. 检查代码是否存在设计文档未记录的行为。
4. 将最终确认的实现变化同步到设计文档。
5. 更新实施状态和验证结果。
6. 仍有未完成或未验证事项时，不得将功能标记为已完成或已验证。

## 长会话交接与新会话恢复

### 交接触发条件

出现以下情况时，应准备实施交接：

- 用户明确要求生成进度总结或交接文档。
- 即将切换到新会话，且当前任务尚未完成。
- 当前实现尚未完成，但已经完成一个明确阶段。
- 当前会话已经较长，后续工作可能无法仅依赖聊天上下文可靠继续。

交接前必须：

1. 将已经确认的设计变化同步到设计文档。
2. 更新实施状态和验证结果。
3. 使用实施交接模板创建或更新交接文档。
4. 确保没有当前聊天记录的新会话，仅依靠设计文档、实施交接和当前代码即可继续。

### 新会话恢复

继续已有版本或功能时：

1. 先读取版本总览、当前功能设计文档和实施交接。
2. 检查当前代码、Git 修改和测试状态。
3. 对比设计目标和实际实现。
4. 开始编码前输出简短恢复结果：
   - 当前目标
   - 已完成内容
   - 部分完成和未完成内容
   - 文档与代码差异
   - 当前风险和待确认事项
   - 下一步计划
5. 恢复结果存在明显冲突或无法确认的信息时，先询问用户。

## 接口文档生成

当用户提出生成、更新、补充或审查接口文档时：

1. 必须使用 `$api-doc-standard`，按照该 Skill 的模板、字段规范、双兼容导航和校验流程执行。
2. 按照 Obsidian 检索规则读取对应版本的当前有效或已确认设计方案。
3. 设计文档用于确认目标行为，当前代码用于确认实际实现；两者不一致时必须明确记录差异。
4. 按以下顺序核实接口：
   1. Controller 路径和 HTTP 方法。
   2. Request、Param、DTO 和校验注解。
   3. Response VO、DTO 和 Schema。
   4. Application、Domain、Service 逻辑中的条件值、过期时间、状态流转和消费语义。
   5. 测试代码中的边界情况和准确枚举值。
5. 不得发明字段、枚举值、可空性、时间单位、过期行为或前端处理动作。
6. 接口文档默认保存到 `F:\笔记\个人知识库\灵犀\接口文档`。
7. 版本接口文档默认命名为 `V<版本号> 接口文档.md`。
8. 生成后必须运行 `$api-doc-standard` 提供的校验脚本，并修复所有错误。
9. 必须确认 Obsidian 和网页版接口跳转均可使用。
10. 只生成或修改接口文档时，不得修改业务代码，除非用户明确要求。

## 发版记录生成

当用户提出生成、更新、补充或审查发版记录，或者明确要求整理生产发版内容时：

1. 必须先读取发版记录模板：
   - `F:\笔记\个人知识库\灵犀\灵犀发版记录模板.md`
   - 模板不存在时，必须先向用户说明并确认是否创建，不得使用其他项目的模板替代。
2. 发版记录默认保存到：
   - `F:\笔记\个人知识库\灵犀\发版记录`
3. 默认命名为 `灵犀v<版本号>.md`；如果版本号已经包含 `v`，不得重复添加。
4. 同一版本已经存在发版记录时，优先更新原文档，不得重复创建。
5. 无法从用户输入、设计文档、实施交接或代码确认版本号时，先向用户确认版本号。
6. 保留模板 YAML、备忘事项、项目变更、脚本变更和配置等核心结构；可以根据实际情况增加发版顺序、验证步骤、回滚方案和发布结果。

### 发版内容事实来源

按以下来源核实发版内容：

1. 用户明确指定的发版范围和版本号。
2. 当前有效或已确认的设计文档。
3. 当前版本的实施交接和验收结果。
4. 各仓库实际 Git 修改、提交记录和当前代码。
5. 已确认的数据库脚本、配置变更和部署文件。
6. 相关编译、测试和静态检查结果。

设计文档描述目标范围，Git 和当前代码描述实际纳入发版的内容；两者不一致时必须显式记录。

### 发版记录填写规则

1. `影响服务` 只能填写本次实际需要发布或受影响的服务，并按照模板中的服务名称和顺序记录。
2. “项目变更”必须说明各服务的实际变更、依赖关系和必要的发布顺序，不得仅复制需求描述。
3. “脚本变更”只能记录已经确认的 SQL、数据迁移或修复脚本，并说明执行顺序、适用环境、前置条件、校验方式和回滚或前向修复方式。
4. 没有脚本变更时明确填写“无”，不得保留可能被误执行的示例 SQL。
5. “配置”必须注明配置文件或 DataId、Group、配置项、变更前后含义、适用环境和生效方式。
6. 不得在发版记录中写入真实密码、密钥、Token、私钥或其他敏感值；敏感配置只记录配置项名称和安全的占位说明。
7. 没有配置变更时明确填写“无”，不得根据设计方案推测生产配置值。
8. 编译、测试、静态检查或验收尚未执行时必须标记为“未验证”，不得写成已通过。
9. 创建文档时默认保持 `状态: 待发布`。
10. 只有用户确认已经完成生产发布，或存在可核实的发布结果时，才填写发版日期并将状态更新为已发布。
11. `是否回滚` 只有在实际发生并确认回滚后才能改为 `true`，同时记录回滚范围、原因和验证结果。
12. 只生成或修改发版记录时，不得修改业务代码、执行 SQL、修改生产配置、创建 Git 标签或触发部署，除非用户明确要求。

### 发版记录交付前检查

- 版本号、文件名和 YAML 中的版本一致。
- 影响服务与实际代码、脚本和配置变更一致。
- 发布顺序和跨服务依赖没有遗漏。
- SQL 和配置内容有明确来源，不包含猜测或敏感值。
- 未验证事项、风险、回滚条件和验证步骤已经显式记录。
- 文档没有残留模板占位符或无效示例。
