services:
  nacos:
    container_name: nacos
    image: nacos:3.0.3
    build:
      context: ./nacos
      dockerfile: dockerfile
    environment:
      - MODE=standalone
      - NACOS_AUTH_ENABLE=true
      - NACOS_AUTH_TOKEN=bmFjb3M=
      - NACOS_AUTH_IDENTITY_KEY=serverIdentity
      - NACOS_AUTH_IDENTITY_VALUE=security
    volumes:
      - ./nacos/logs/:/home/nacos/logs
    ports:
      - "8846:8846"
      - "8848:8848"
      - "9848:9848"
      - "9849:9849"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - milvus

  milvus:
    container_name: milvus
    image: milvusdb/milvus:v2.6.12
    command: ["milvus", "run", "standalone"]
    security_opt:
      - seccomp:unconfined
    environment:
      - TZ=Asia/Shanghai
      - MINIO_REGION=us-east-1
      - ETCD_ENDPOINTS=etcd:2379
      - MINIO_ADDRESS=minio:9000
    ports:
      # Milvus gRPC (langchain4j-milvus 默认会连这个端口)
      - "19530:19530"
      # 可选：方便你后续查看服务/排查
      - "9091:9091"
      - "19121:19121"
      - "8080:8080"
    volumes:
      - milvus-data:/var/lib/milvus
    depends_on:
      - etcd
      - minio
    restart: unless-stopped

  etcd:
    container_name: milvus-etcd
    image: quay.io/coreos/etcd:v3.5.25
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    command: etcd -advertise-client-urls=http://etcd:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    volumes:
      - etcd-data:/etcd

  minio:
    container_name: milvus-minio
    image: minio/minio:RELEASE.2024-05-28T17-19-04Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    ports:
      - "9001:9001"
      - "9000:9000"
    command: minio server /minio_data --console-address ":9001"
    volumes:
      - minio-data:/minio_data
  attu:
    container_name: milvus-attu
    image: zilliz/attu:latest
    ports:
      - "3000:3000"
    environment:
      - MILVUS_URL=milvus:19530
    depends_on:
      - milvus
    restart: unless-stopped
  #   depends_on:
  #     - lingxi-mysql
  # lingxi-mysql:
  #   container_name: lingxi-mysql
  #   image: mysql:5.7
  #   build:
  #     context: ./mysql
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - ./mysql/conf:/etc/mysql/conf.d
  #     - ./mysql/logs:/logs
  #     - ./mysql/data:/var/lib/mysql
  #   command: [
  #         'mysqld',
  #         '--innodb-buffer-pool-size=80M',
  #         '--character-set-server=utf8mb4',
  #         '--collation-server=utf8mb4_unicode_ci',
  #         '--default-time-zone=+8:00',
  #         '--lower-case-table-names=1'
  #       ]
  #   environment:
  #     MYSQL_DATABASE: 'ry-cloud'
  #     MYSQL_ROOT_PASSWORD: password
  # lingxi-redis:
  #   container_name: lingxi-redis
  #   image: redis
  #   build:
  #     context: ./redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - ./redis/conf/redis.conf:/home/cloud/redis/redis.conf
  #     - ./redis/data:/data
  #   command: redis-server /home/cloud/redis/redis.conf
  # lingxi-nginx:
  #   container_name: lingxi-nginx
  #   image: nginx
  #   build:
  #     context: ./nginx
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx/html/dist:/home/cloud/projects/lingxi-ui
  #     - ./nginx/conf/nginx.conf:/etc/nginx/nginx.conf
  #     - ./nginx/logs:/var/log/nginx
  #     - ./nginx/conf.d:/etc/nginx/conf.d
  #   depends_on:
  #     - cloud-gateway
  #   links:
  #     - cloud-gateway
  # cloud-gateway:
  #   container_name: cloud-gateway
  #   build:
  #     context: ./cloud/gateway
  #     dockerfile: dockerfile
  #   ports:
  #     - "8888:8888"
  #   depends_on:
  #     - cloud-redis
  #   links:
  #     - cloud-redis
  # cloud-auth:
  #   container_name: cloud-auth
  #   build:
  #     context: ./cloud/auth
  #     dockerfile: dockerfile
  #   ports:
  #     - "9200:9200"
  #   depends_on:
  #     - cloud-redis
  #   links:
  #     - cloud-redis
  # cloud-modules-system:
  #   container_name: cloud-modules-system
  #   build:
  #     context: ./cloud/modules/system
  #     dockerfile: dockerfile
  #   ports:
  #     - "9201:9201"
  #   depends_on:
  #     - cloud-redis
  #     - cloud-mysql
  #   links:
  #     - cloud-redis
  #     - cloud-mysql
  # cloud-modules-gen:
  #   container_name: cloud-modules-gen
  #   build:
  #     context: ./cloud/modules/gen
  #     dockerfile: dockerfile
  #   ports:
  #     - "9202:9202"
  #   depends_on:
  #     - cloud-mysql
  #   links:
  #     - cloud-mysql
  # cloud-modules-job:
  #   container_name: cloud-modules-job
  #   build:
  #     context: ./cloud/modules/job
  #     dockerfile: dockerfile
  #   ports:
  #     - "9203:9203"
  #   depends_on:
  #     - cloud-mysql
  #   links:
  #     - cloud-mysql
  # cloud-modules-file:
  #   container_name: cloud-modules-file
  #   build:
  #     context: ./cloud/modules/file
  #     dockerfile: dockerfile
  #   ports:
  #     - "9300:9300"
  #   volumes:
  #   - ./cloud/uploadPath:/home/cloud/uploadPath
  # cloud-visual-monitor:
  #   container_name: cloud-visual-monitor
  #   build:
  #     context: ./cloud/visual/monitor
  #     dockerfile: dockerfile
  #   ports:
  #     - "9100:9100"

volumes:
  milvus-data:
  etcd-data:
  minio-data:
